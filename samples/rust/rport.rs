use kernel::learn::uart_port::UartPort;

pub struct RPort{
}

impl RPort{
    pub fn new() -> Self{
        RPort{
        }
    }

}


pub(crate) fn uart_port_init(port: &UartPort) {
    unsafe {






    }
}